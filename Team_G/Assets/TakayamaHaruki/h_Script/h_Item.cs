using UnityEngine;

public class h_Item : MonoBehaviour
{
    public Rigidbody rb;
    public float fall_Velocity = 0.0f;
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
           
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
